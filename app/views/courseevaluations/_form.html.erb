<%= form_with(model: courseevaluation, local: true) do |form| %>
  <% if courseevaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(courseevaluation.errors.count, "error") %> prohibited this courseevaluation from being saved:</h2>

      <ul>
      <% courseevaluation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.collection_select :course_id, Course.all, :id, :Number %>
  </div>

  <div class="field">
    <%= form.label :faculty_id %>
    <%= form.collection_select :faculty_id, Faculty.all, :id, :full_name %>
  </div>

  <div class="field">
    <%= form.label :criterion_id %>
    <%= form.collection_select :criterion_id, Criterion.all, :id, :Title %>
  </div>

  <div class="field">
    <%= form.label :criteriathreshold %>
    <%= form.number_field :criteriathreshold %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
